import { useState } from 'react';
import {
  Activity, AlertTriangle, ArrowDownToLine, Ban, Check, CheckCircle2,
  ChevronRight, CircleDollarSign, Clock3, LayoutDashboard, LogOut,
  ShieldCheck, UserCheck, UserPlus, Users, Wallet,
} from 'lucide-react';
import { formatKES } from '@/types';

interface AdminDashboardProps { onLogout: () => void; }
type MemberStatus = 'Verified' | 'Pending' | 'Blocked';
type Member = { id: number; name: string; phone: string; plan: string; status: MemberStatus; group: string; paid: boolean };
type ActivityItem = { id: number; text: string; group: string; time: string };

const groups = ['Group #4821', 'Group #4818', 'Group #4804', 'Unassigned'];
const seedMembers: Member[] = [
  { id: 1, name: 'Wanjiru Kamau', phone: '+254 712 345 142', plan: 'Standard', status: 'Verified', group: 'Group #4821', paid: true },
  { id: 2, name: 'Amara Otieno', phone: '+254 712 345 883', plan: 'Premium', status: 'Verified', group: 'Group #4818', paid: true },
  { id: 3, name: 'Brian Kiptoo', phone: '+254 712 345 251', plan: 'Starter', status: 'Pending', group: 'Unassigned', paid: false },
  { id: 4, name: 'Fatuma Noor', phone: '+254 712 345 617', plan: 'Standard', status: 'Verified', group: 'Group #4821', paid: false },
  { id: 5, name: 'David Mwangi', phone: '+254 712 345 390', plan: 'Standard', status: 'Pending', group: 'Unassigned', paid: false },
];
const seedActivity: ActivityItem[] = [
  { id: 1, text: 'Wanjiru Kamau paid the monthly contribution', group: 'Group #4821', time: '2 min ago' },
  { id: 2, text: 'Payout of KES 25,000 released to Mercy Akinyi', group: 'Group #4790', time: '18 min ago' },
  { id: 3, text: 'Amara Otieno joined Group #4818', group: 'Group #4818', time: '42 min ago' },
  { id: 4, text: 'Brian Kiptoo submitted a KYC application', group: 'Unassigned', time: '1 hr ago' },
];

export function AdminDashboard({ onLogout }: AdminDashboardProps) {
  const [tab, setTab] = useState('Overview');
  const [members, setMembers] = useState(seedMembers);
  const [activity, setActivity] = useState(seedActivity);
  const [unpaidOnly, setUnpaidOnly] = useState(false);
  const [notice, setNotice] = useState('');
  const pending = members.filter((member) => member.status === 'Pending').length;
  const unpaid = members.filter((member) => !member.paid && member.status !== 'Blocked').length;
  const tabs = [
    { label: 'Overview', icon: LayoutDashboard }, { label: 'Members', icon: Users },
    { label: 'Groups', icon: UserPlus }, { label: 'Payments', icon: Wallet },
    { label: 'Activity log', icon: Activity },
  ];

  const changeMember = (member: Member, update: Partial<Member>, text: string) => {
    setMembers((current) => current.map((item) => item.id === member.id ? { ...item, ...update } : item));
    setActivity((current) => [{ id: Date.now(), text, group: member.group, time: 'Just now' }, ...current]);
    setNotice(text);
  };

  return (
    <div className="min-h-[100dvh] bg-ink-50 text-ink-900">
      <header className="border-b border-ink-100 bg-white"><div className="mx-auto flex max-w-screen-2xl items-center justify-between gap-4 px-4 py-4 sm:px-8 lg:px-12"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-brand-700"><span className="font-extrabold text-gold-400">C</span></div><div><p className="font-extrabold">Chama Admin</p><p className="text-xs text-ink-400">Full system operations</p></div></div><div className="flex items-center gap-4"><span className="hidden items-center gap-2 text-sm text-ink-500 sm:flex"><span className="h-2.5 w-2.5 rounded-full bg-brand-500" />All systems operational</span><button onClick={onLogout} className="btn-secondary flex items-center gap-2 px-3 py-2.5 text-sm"><LogOut size={16} /><span className="hidden sm:inline">Log out</span></button></div></div></header>
      <main className="mx-auto max-w-screen-2xl px-4 py-6 sm:px-8 sm:py-8 lg:px-12">
        <div className="mb-7"><p className="mb-2 text-sm font-semibold text-brand-600">Operations console · Friday, September 4, 2026</p><h1 className="text-3xl font-extrabold tracking-tight sm:text-4xl">Good morning, Admin</h1><p className="mt-2 text-sm text-ink-500">Manage members, groups, payments, and every activity across the platform.</p></div>
        <div className="mb-7 overflow-x-auto border-b border-ink-200"><div className="flex min-w-max gap-1">{tabs.map(({ label, icon: Icon }) => <button key={label} onClick={() => setTab(label)} className={`flex items-center gap-2 border-b-2 px-4 py-3 text-sm font-semibold ${tab === label ? 'border-brand-600 text-brand-700' : 'border-transparent text-ink-400 hover:text-ink-700'}`}><Icon size={17} />{label}</button>)}</div></div>
        {notice && <div className="mb-5 flex items-center justify-between rounded-xl border border-brand-200 bg-brand-50 px-4 py-3 text-sm font-semibold text-brand-700"><span className="flex items-center gap-2"><CheckCircle2 size={17} />{notice}</span><button onClick={() => setNotice('')}>×</button></div>}
        {tab === 'Overview' && <Overview members={members} pending={pending} unpaid={unpaid} activity={activity} go={setTab} />}
        {tab === 'Members' && <Members members={members} onAccept={(member) => changeMember(member, { status: 'Verified' }, `${member.name} was accepted into Chama`)} onBlock={(member) => changeMember(member, { status: 'Blocked' }, `${member.name} was blocked from the system`)} onAssign={(member, group) => changeMember(member, { group }, `${member.name} assigned to ${group}`)} />}
        {tab === 'Groups' && <Groups members={members} />}
        {tab === 'Payments' && <Payments members={members} unpaidOnly={unpaidOnly} setUnpaidOnly={setUnpaidOnly} onPay={(member) => changeMember(member, { paid: true }, `Advance contribution paid for ${member.name}`)} />}
        {tab === 'Activity log' && <ActivityLog activity={activity} />}
        <footer className="py-8 text-center text-xs text-ink-400">Chama Admin Console · Demo data with live local controls</footer>
      </main>
    </div>
  );
}

function Overview({ members, pending, unpaid, activity, go }: { members: Member[]; pending: number; unpaid: number; activity: ActivityItem[]; go: (tab: string) => void }) {
  return <><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Total members" value="1,248" detail="+8.4% this month" icon={Users} tone="brand" /><Metric label="Active groups" value="186" detail="12 groups forming" icon={Activity} tone="gold" /><Metric label="Funds managed" value="KES 8.4M" detail="+12.6% this month" icon={CircleDollarSign} tone="ink" /><Metric label="Needs attention" value={`${pending + unpaid}`} detail={`${pending} approvals · ${unpaid} unpaid`} icon={AlertTriangle} tone="red" /></section><section className="mt-6 grid gap-6 xl:grid-cols-[minmax(0,1.35fr)_minmax(320px,0.65fr)]"><div className="card p-5 sm:p-6"><div className="flex items-center justify-between"><div><h2 className="font-bold">Contribution volume</h2><p className="mt-1 text-xs text-ink-400">Total contributions over the last 7 days</p></div><span className="badge bg-brand-100 text-brand-700">This week</span></div><div className="mt-5 flex items-end gap-3"><p className="text-3xl font-extrabold">KES 1.26M</p><p className="text-xs font-semibold text-brand-600">+18.2%</p></div><div className="mt-7 flex h-44 items-end gap-2 sm:gap-4">{[42, 58, 48, 76, 64, 88, 72].map((height, index) => <div key={index} className="flex h-full flex-1 flex-col justify-end gap-2"><div className={`rounded-t-lg ${index === 5 ? 'bg-gold-400' : 'bg-brand-500'}`} style={{ height: `${height}%` }} /><span className="text-center text-[10px] text-ink-400">{['Sat', 'Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri'][index]}</span></div>)}</div></div><div className="card p-5 sm:p-6"><h2 className="font-bold">Quick actions</h2><p className="mt-1 text-xs text-ink-400">Jump into daily operations</p><div className="mt-5 space-y-3"><Quick label="Review members" count={`${pending} pending approvals`} icon={UserCheck} onClick={() => go('Members')} /><Quick label="Monitor contributions" count={`${unpaid} unpaid this month`} icon={Wallet} onClick={() => go('Payments')} /><Quick label="View group activity" count="Live platform events" icon={Activity} onClick={() => go('Activity log')} /></div></div></section><section className="mt-6 grid gap-6 lg:grid-cols-2"><div className="card p-5 sm:p-6"><div className="flex items-center justify-between"><div><h2 className="font-bold">Unpaid contributions</h2><p className="mt-1 text-xs text-ink-400">Members who need a monthly payment</p></div><button onClick={() => go('Payments')} className="text-sm font-semibold text-brand-600">Manage</button></div><div className="mt-4 space-y-3">{members.filter((member) => !member.paid).map((member) => <div key={member.id} className="flex justify-between gap-3"><div><p className="text-sm font-semibold">{member.name}</p><p className="text-xs text-ink-400">{member.group}</p></div><span className="badge bg-gold-100 text-gold-700">Unpaid</span></div>)}</div></div><div className="card p-5 sm:p-6"><div className="flex items-center justify-between"><div><h2 className="font-bold">Latest activity</h2><p className="mt-1 text-xs text-ink-400">What is happening across groups</p></div><button onClick={() => go('Activity log')} className="text-sm font-semibold text-brand-600">View all</button></div><div className="mt-4 space-y-3">{activity.slice(0, 3).map((item) => <ActivityRow key={item.id} item={item} />)}</div></div></section></>;
+}
+
+function Members({ members, onAccept, onBlock, onAssign }: { members: Member[]; onAccept: (member: Member) => void; onBlock: (member: Member) => void; onAssign: (member: Member, group: string) => void }) {
+  return <section className="card overflow-hidden"><Header title="Member management" description="Accept, assign, verify, or block accounts from the system" icon={Users} /><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-ink-50 text-xs uppercase tracking-wide text-ink-400"><tr><th className="px-5 py-3">Member</th><th className="px-5 py-3">Plan</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Group assignment</th><th className="px-5 py-3">Controls</th></tr></thead><tbody className="divide-y divide-ink-100">{members.map((member) => <tr key={member.id} className="hover:bg-ink-50"><td className="px-5 py-4"><p className="font-semibold">{member.name}</p><p className="text-xs text-ink-400">{member.phone}</p></td><td className="px-5 py-4 text-ink-600">{member.plan}</td><td className="px-5 py-4"><Status status={member.status} /></td><td className="px-5 py-4"><select value={member.group} onChange={(event) => onAssign(member, event.target.value)} className="rounded-lg border border-ink-200 bg-white px-3 py-2 text-xs font-semibold"><option>{groups[0]}</option><option>{groups[1]}</option><option>{groups[2]}</option><option>{groups[3]}</option></select></td><td className="px-5 py-4"><div className="flex items-center gap-2">{member.status === 'Pending' && <button onClick={() => onAccept(member)} className="flex items-center gap-1 rounded-lg bg-brand-600 px-3 py-2 text-xs font-bold text-white"><Check size={14} />Accept</button>}{member.status !== 'Blocked' && <button onClick={() => onBlock(member)} className="flex items-center gap-1 rounded-lg border border-red-200 px-3 py-2 text-xs font-bold text-red-600"><Ban size={14} />Block</button>}{member.status === 'Blocked' && <span className="text-xs font-semibold text-red-600">Access disabled</span>}</div></td></tr>)}</tbody></table></div></section>;
+}
+
+function Groups({ members }: { members: Member[] }) { return <section className="grid gap-6 lg:grid-cols-3">{groups.slice(0, 3).map((group, index) => { const assigned = members.filter((member) => member.group === group); return <div key={group} className="card p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-brand-600">Active group</p><h2 className="mt-1 text-xl font-extrabold">{group}</h2></div><span className="badge bg-brand-100 text-brand-700">{assigned.length}/5</span></div><p className="mt-2 text-sm text-ink-400">Monthly pool KES {index === 0 ? '25,000' : '50,000'}</p><div className="mt-5 space-y-3">{assigned.length ? assigned.map((member) => <div key={member.id} className="flex justify-between gap-2"><div><p className="text-sm font-semibold">{member.name}</p><p className="text-xs text-ink-400">{member.paid ? 'Paid this month' : 'Contribution pending'}</p></div>{!member.paid && <span className="badge bg-gold-100 text-gold-700">Unpaid</span>}</div>) : <p className="rounded-lg bg-ink-50 p-3 text-sm text-ink-400">No members assigned yet.</p>}</div><p className="mt-5 border-t border-ink-100 pt-4 text-xs font-semibold text-ink-400">Assign members from the Members tab</p></div>; })}<div className="card border-dashed p-5"><div className="flex h-full min-h-48 flex-col items-center justify-center text-center"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-brand-100 text-brand-700"><UserPlus size={20} /></div><h2 className="mt-4 font-bold">Form a new group</h2><p className="mt-2 max-w-xs text-sm text-ink-400">Create a group when five approved members are ready.</p><button className="btn-primary mt-5 px-4 py-2.5 text-sm">Create group</button></div></div></section>; }
+
+function Payments({ members, unpaidOnly, setUnpaidOnly, onPay }: { members: Member[]; unpaidOnly: boolean; setUnpaidOnly: (value: boolean) => void; onPay: (member: Member) => void }) { const list = members.filter((member) => !unpaidOnly || (!member.paid && member.status !== 'Blocked')); return <section className="space-y-6"><div className="grid gap-4 sm:grid-cols-3"><Metric label="Due this month" value={`KES ${(members.length * 5000).toLocaleString()}`} detail="Across active groups" icon={CircleDollarSign} tone="brand" /><Metric label="Collected" value={`KES ${(members.filter((member) => member.paid).length * 5000).toLocaleString()}`} detail="Current cycle" icon={CheckCircle2} tone="gold" /><Metric label="Unpaid members" value={`${members.filter((member) => !member.paid).length}`} detail="Needs follow-up" icon={Clock3} tone="red" /></div><div className="card overflow-hidden"><Header title="Contribution monitor" description="Track payments and cover a member in advance" icon={Wallet} action={<button onClick={() => setUnpaidOnly(!unpaidOnly)} className="rounded-lg border border-ink-200 px-3 py-2 text-xs font-bold text-ink-600">{unpaidOnly ? 'Showing unpaid only' : 'Show unpaid only'}</button>} /><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-ink-50 text-xs uppercase tracking-wide text-ink-400"><tr><th className="px-5 py-3">Member</th><th className="px-5 py-3">Group</th><th className="px-5 py-3">Amount due</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Admin action</th></tr></thead><tbody className="divide-y divide-ink-100">{list.map((member) => <tr key={member.id}><td className="px-5 py-4"><p className="font-semibold">{member.name}</p><p className="text-xs text-ink-400">{member.phone}</p></td><td className="px-5 py-4 text-ink-600">{member.group}</td><td className="px-5 py-4 font-bold">KES 5,000</td><td className="px-5 py-4">{member.paid ? <span className="badge bg-brand-100 text-brand-700"><CheckCircle2 size={12} />Paid</span> : <span className="badge bg-gold-100 text-gold-700"><Clock3 size={12} />Pending</span>}</td><td className="px-5 py-4">{member.paid ? <span className="text-xs font-semibold text-ink-400">No action needed</span> : <button onClick={() => onPay(member)} className="flex items-center gap-1 rounded-lg bg-brand-600 px-3 py-2 text-xs font-bold text-white"><ArrowDownToLine size={14} />Pay in advance</button>}</td></tr>)}</tbody></table></div></div></section>; }
+
+function ActivityLog({ activity }: { activity: ActivityItem[] }) { return <section className="card overflow-hidden"><Header title="Full activity log" description="Monitor every member, group, payment, and system event" icon={Activity} action={<span className="badge bg-brand-100 text-brand-700">Live monitoring</span>} /><div className="divide-y divide-ink-100">{[...activity, ...activity].map((item, index) => <div key={`${item.id}-${index}`} className="flex items-start gap-4 p-5 hover:bg-ink-50"><div className="mt-1 flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-100 text-brand-700"><Activity size={18} /></div><div><p className="text-sm font-semibold">{item.text}</p><p className="mt-1 text-xs text-ink-400">{item.group} · {item.time}</p></div></div>)}</div></section>; }
+
+function Header({ title, description, icon: Icon, action }: { title: string; description: string; icon: typeof Users; action?: React.ReactNode }) { return <div className="flex flex-col gap-4 border-b border-ink-100 p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-brand-100 text-brand-700"><Icon size={19} /></div><div><h2 className="font-bold">{title}</h2><p className="mt-1 text-xs text-ink-400">{description}</p></div></div>{action}</div>; }
+function ActivityRow({ item }: { item: ActivityItem }) { return <div className="flex items-center gap-3"><span className="h-2 w-2 rounded-full bg-brand-500" /><div><p className="text-sm font-semibold">{item.text}</p><p className="mt-1 text-xs text-ink-400">{item.time}</p></div></div>; }
+function Status({ status }: { status: MemberStatus }) { const color = status === 'Verified' ? 'bg-brand-100 text-brand-700' : status === 'Pending' ? 'bg-gold-100 text-gold-700' : 'bg-red-100 text-red-600'; return <span className={`badge ${color}`}>{status === 'Verified' ? <ShieldCheck size={12} /> : status === 'Blocked' ? <Ban size={12} /> : <Clock3 size={12} />}{status}</span>; }
+function Quick({ icon: Icon, label, count, onClick }: { icon: typeof Users; label: string; count: string; onClick: () => void }) { return <button onClick={onClick} className="flex w-full items-center gap-3 rounded-xl bg-ink-50 p-3 text-left hover:bg-brand-50"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-white text-brand-600 shadow-sm"><Icon size={17} /></div><span className="flex-1"><span className="block text-sm font-semibold">{label}</span><span className="block text-xs text-ink-400">{count}</span></span><ChevronRight size={16} className="text-ink-300" /></button>; }
+function Metric({ label, value, detail, icon: Icon, tone }: { label: string; value: string; detail: string; icon: typeof Users; tone: 'brand' | 'gold' | 'ink' | 'red' }) { const colors = { brand: 'bg-brand-100 text-brand-700', gold: 'bg-gold-100 text-gold-700', ink: 'bg-ink-100 text-ink-700', red: 'bg-red-100 text-red-600' }; return <div className="card p-5 sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-sm text-ink-500">{label}</p><p className="mt-2 text-2xl font-extrabold">{value}</p></div><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${colors[tone]}`}><Icon size={19} /></div></div><p className="mt-4 text-xs font-semibold text-ink-400">{detail}</p></div>; }
